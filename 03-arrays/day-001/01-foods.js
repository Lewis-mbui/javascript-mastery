// Create an array of your 5 favorite foods.

// Print the first, middle, and last item from the array.

// Add one new food at the end and one at the beginning.

const foods = ['mukimo', 'fries', 'omena', 'matoke', 'chicken'];

console.log(foods[0]);
console.log(foods[2]);
console.log(foods[4]);