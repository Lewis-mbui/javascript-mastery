/*
Use filter() to get a new array of people who are 18 or older.

Print the result.
*/

const ages = [12, 18, 25, 30, 40, 15, 22];

const filtered = ages.filter((age) => age >= 18);

console.log(ages);
console.log(filtered);