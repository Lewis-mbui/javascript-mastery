// Write a function that takes a name as 
// input and returns a personalized greeting message.

const myName = 'Lewis';
console.log(greet(myName));

function greet(name) {
  return "Hello " + name;
}