/* 
Write an arrow function that takes a first name and 
last name and returns a formatted full name like "John Doe".
*/

const formatName = (firstName, lastName) => firstName + ' ' + lastName;

console.log(formatName('John', 'Doe'));